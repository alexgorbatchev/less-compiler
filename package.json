{
  "name": "less-compiler",
  "main": "./less-compiler.js",
  "version": "0.2.3",
  "description": "A handy LESS compiler from a series of compilers that have the same signature.",
  "keywords": ["less", "css", "compiler"],
  "repository": {
    "type": "git",
    "url": "https://github.com/alexgorbatchev/less-compiler.git"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha test/*.spec.coffee",
    "pretest": "./node_modules/.bin/coffee --bare --compile *.coffee"
  },
  "peerDependencies": {
    "less": ">=1.x.x"
  },
  "devDependencies": {
    "less": "~1.5.1",
    "coffee-script": "~1.6.3",
    "chai": "~1.6.0",
    "mocha": "~1.10.0",
    "fibrous": "~0.3.3"
  }
}
